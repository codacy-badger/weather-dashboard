import { DayOrNightPipe } from './day-or-night.pipe';

describe('DayOrNightPipe', () => {
  it('create an instance', () => {
    const pipe = new DayOrNightPipe();
    expect(pipe).toBeTruthy();
  });
});
