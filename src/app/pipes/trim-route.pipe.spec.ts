import { TrimRoutePipe } from './trim-route.pipe';

describe('TrimRoutePipe', () => {
  it('create an instance', () => {
    const pipe = new TrimRoutePipe();
    expect(pipe).toBeTruthy();
  });
});
